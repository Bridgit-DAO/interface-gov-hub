#!/usr/bin/env python3
"""Legacy script - no longer needed.

Build number is now in config.py (load_build_number) and passed via
services/rendering.configure_rendering() and _format_base_template().
"""
print("Build number is handled in config.py and services/rendering.py. No action needed.")
